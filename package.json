{
  "name": "7summit-nav",
  "version": "1.0.0",
  "private": true,
  "description": "Navigasi + menu fullscreen 7Summit Indonesia, dibangun dengan Tailwind CSS v4 CLI",
  "scripts": {
    "dev": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/output.css --watch",
    "build": "npx @tailwindcss/cli -i ./src/input.css -o ./dist/output.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}